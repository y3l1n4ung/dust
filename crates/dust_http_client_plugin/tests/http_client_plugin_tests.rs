//! Integration tests for HTTP client plugin validation and emission.

mod http_client_plugin_tests {
    mod emission;
    mod emission_request;
    mod emission_response;
    mod emission_test_file;
    mod registration;
    mod support;
    mod validation;
    mod validation_class;
    mod validation_endpoint;
    mod validation_param;
}
