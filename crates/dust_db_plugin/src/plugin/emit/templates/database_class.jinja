final class {{ generated_name }} implements {{ class_name }} {
  {{ generated_name }}._(this._driver);

  factory {{ generated_name }}.open(
    String path, {
    SqliteConnectOptions? options,
  }) {
    final driver = {{ open_expr }};
    return {{ generated_name }}._(driver);
  }

  final {{ driver_type }} _driver;

  @override
  Connection get connection => _driver;

  @override
  UnsafeSql get unsafe => {{ unsafe_expr }};

  Pool get pool => _driver;
}

{{ migrations }}
