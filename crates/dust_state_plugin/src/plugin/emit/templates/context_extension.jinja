/// BuildContext helpers generated for {{ view_model_class }}.
///
/// ```dart
/// final vm = context.{{ read_name }}();
/// final state = context.{{ watch_name }}().value;
/// ```
extension {{ extension_class }} on BuildContext {
  {{ proxy_class }} {{ watch_name }}() {
    return {{ proxy_class }}(this);
  }

  {{ view_model_class }} {{ read_name }}() => {{ scope_class }}.read(this);
}
