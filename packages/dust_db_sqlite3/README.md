# dust_db_sqlite3

Native SQLite runtime for Database code generated by
[Dust](https://github.com/y3l1n4ung/dust).

This package implements the driver-independent `Executor`, `Pool`, and `Row`
contracts from `dust_dart` using `package:sqlite3`.

Database is beta. It uses raw SQL with build-time validation; it is not an ORM
or query builder.

## Installation

Add the Database annotations and SQLite runtime:

```bash
dart pub add dust_dart dust_db_sqlite3
```

Install the Dust CLI by following the
[main installation guide](https://github.com/y3l1n4ung/dust#installation).

The driver uses Dart FFI and supports native Dart and Flutter targets. It is not
intended for web applications.

## Quick Start

Open a database and apply named migrations:

```dart
import 'package:dust_dart/db.dart';
import 'package:dust_db_sqlite3/dust_db_sqlite3.dart';

Future<void> main() async {
  final db = Sqlite3Driver.open(
    'app.db',
    migrations: const {
      '0001_create_users.sql': '''
CREATE TABLE users (
  id INTEGER PRIMARY KEY,
  name TEXT NOT NULL
);
''',
    },
  );

  try {
    await queryExecute(
      'INSERT INTO users (name) VALUES (?)',
      ['Ada'],
    ).execute(db);

    final count = await queryScalar<int>(
      'SELECT COUNT(*) FROM users',
      const [],
    ).fetchOne(db);

    print('users: $count');
  } finally {
    await db.close();
  }
}
```

Migrations run in sorted filename order. Applied names are stored in
`__dust_schema_migrations`, so reopening the database applies only new files.
Generated `@SqlxDatabase` openers embed the configured migration directory and
create the same `Sqlite3Driver` automatically.

## Generated Database Code

Application queries normally live in `@SqlxDao` methods. Dust validates those
queries and generates typed calls against this driver:

```bash
dust build
dust db build
dust check --db
```

Open the generated database and pass its pool to generated DAOs:

```dart
final database = AppDatabase.open('app.db');
final users = UserDao(database.pool);

final result = await users.findById(42);
await database.pool.close();
```

See the [Database guide](https://github.com/y3l1n4ung/dust/blob/main/docs/usage/db.md)
for migrations, row mapping, DAO return types, placeholders, and offline
validation.

## Transactions

Transactions commit on `Ok` and roll back on `Err` or a thrown exception:

```dart
final result = await database.pool.transaction((tx) async {
  return UserDao(tx).createUser('ada@example.com', 'Ada');
});
```

## Raw SQLite Access

Use `raw` for dynamic SQL that cannot be checked during generation:

```dart
final rows = await database.pool.raw.fetch(
  'SELECT * FROM users WHERE id = ?',
  [id],
);
```

Access the underlying `package:sqlite3` database only for driver-specific
operations:

```dart
final sqlite = (database.pool as Sqlite3Executor).database;
```

Prefer generated DAOs for product queries because raw and native access do not
receive Dust's build-time SQL validation.

## Documentation

- [Database guide](https://github.com/y3l1n4ung/dust/blob/main/docs/usage/db.md)
- [Shopping app database](https://github.com/y3l1n4ung/dust/blob/main/examples/shopping_app/lib/core/db/shopping_cache_database.dart)
- [Package example](https://github.com/y3l1n4ung/dust/blob/main/packages/dust_db_sqlite3/example/dust_db_sqlite3_example.dart)

Report problems through the
[Dust issue tracker](https://github.com/y3l1n4ung/dust/issues). Contributions
follow the repository's
[contributor guide](https://github.com/y3l1n4ung/dust/blob/main/CONTRIBUTING.md).

Licensed under the
[MIT License](https://github.com/y3l1n4ung/dust/blob/main/LICENSE).
