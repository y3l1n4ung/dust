# Changelog

## 0.1.0

- Initial public release of Dust serde annotations.
- Added `Serialize`, `Deserialize`, and `SerDe`.
- Added `SerDeRename` for Rust-style rename rules.
- Added `SerDeCodec<DartT, JsonT>` for custom field conversion.
