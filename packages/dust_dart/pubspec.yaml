name: dust_dart
description: Unified Dart runtime and annotations for Dust code generation.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  collection: ^1.19.1
  dio: ^5.9.2
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
