name: dust_db
description: Database annotations for Dust sqflite repository generation.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
