name: dust_db_runtime
description: Runtime contracts and query helpers for Dust DB.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  dust_db_annotation:
    path: ../dust_db_annotation

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
