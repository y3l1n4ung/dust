import 'package:flutter/material.dart';

class EmptyShellPage extends StatelessWidget {
  const EmptyShellPage({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
