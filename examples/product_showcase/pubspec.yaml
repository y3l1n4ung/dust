name: product_showcase
description: Real Dust example package with multiple annotated models.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

resolution: workspace

dependencies:
  derive_annotation:
    path: ../../packages/derive_annotation
  derive_serde_annotation:
    path: ../../packages/derive_serde_annotation

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
