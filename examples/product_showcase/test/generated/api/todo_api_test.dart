// GENERATED CODE - DO NOT MODIFY BY HAND
// coverage:ignore-file
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, deprecated_member_use
// ignore_for_file: deprecated_member_use_from_same_package
// ignore_for_file: use_function_type_syntax_for_parameters
// ignore_for_file: unnecessary_const, avoid_init_to_null
// ignore_for_file: invalid_override_different_default_values_named
// ignore_for_file: prefer_expression_function_bodies
// ignore_for_file: annotate_overrides, invalid_annotation_target
// ignore_for_file: unnecessary_question_mark
// Generated by dust.
import 'package:dio/dio.dart';
import 'package:test/test.dart';
import 'dart:isolate';
import 'package:product_showcase/models/todo.dart';
import 'package:product_showcase/api/todo_api.dart';

void main() {
  group('TodoApi request mapping', () {
    test('GET list', () async {
      RequestOptions? captured;
      final dio = Dio();
      dio.interceptors.add(
        InterceptorsWrapper(
          onRequest: (options, handler) {
            captured = options;
            handler.resolve(Response<dynamic>(requestOptions: options, data: const <dynamic>[]));
          },
        ),
      );
      final api = TodoApi(dio);
      try {
        await api.list(userId: 42, page: 42, traceId: 'dust-id');
      } catch (_) {}
      expect(captured, isNotNull);
      final request = captured!;
      expect(request.method, 'GET');
      expect(request.path, '/todos');
      expect(request.queryParameters, isA<Map<String, dynamic>>());
      expect(request.headers, isA<Map<String, dynamic>>());
      expect(request.queryParameters['userId'], equals(42));
      expect(request.queryParameters['page'], equals(42));
      expect(request.headers['x-trace-id'], equals('dust-id'));
      expect(request.headers['x-api-version'], equals('2026-05'));
      expect(request.headers['x-endpoint'], equals('todos'));
    });    test('GET fetchRaw', () async {
      RequestOptions? captured;
      final dio = Dio();
      dio.interceptors.add(
        InterceptorsWrapper(
          onRequest: (options, handler) {
            captured = options;
            handler.resolve(Response<dynamic>(requestOptions: options, data: const <String, dynamic>{}));
          },
        ),
      );
      final api = TodoApi(dio);
      try {
        await api.fetchRaw('dust-id', cancelToken: null, retryable: true);
      } catch (_) {}
      expect(captured, isNotNull);
      final request = captured!;
      expect(request.method, 'GET');
      expect(request.path, '/todos/dust-id');
      expect(request.queryParameters, isA<Map<String, dynamic>>());
      expect(request.headers, isA<Map<String, dynamic>>());
      expect(request.headers['x-api-version'], equals('2026-05'));
    });    test('PATCH rename', () async {
      RequestOptions? captured;
      final dio = Dio();
      dio.interceptors.add(
        InterceptorsWrapper(
          onRequest: (options, handler) {
            captured = options;
            handler.resolve(Response<dynamic>(requestOptions: options, data: const <String, dynamic>{}));
          },
        ),
      );
      final api = TodoApi(dio);
      try {
        await api.rename('dust-id', 'dust-id');
      } catch (_) {}
      expect(captured, isNotNull);
      final request = captured!;
      expect(request.method, 'PATCH');
      expect(request.path, '/todos/dust-id');
      expect(request.queryParameters, isA<Map<String, dynamic>>());
      expect(request.headers, isA<Map<String, dynamic>>());
      expect((request.data as Map<String, dynamic>)['title'], equals('dust-id'));
      expect(request.headers['x-api-version'], equals('2026-05'));
    });    test('DELETE delete', () async {
      RequestOptions? captured;
      final dio = Dio();
      dio.interceptors.add(
        InterceptorsWrapper(
          onRequest: (options, handler) {
            captured = options;
            handler.resolve(Response<dynamic>(requestOptions: options, data: null));
          },
        ),
      );
      final api = TodoApi(dio);
      try {
        await api.delete('dust-id', audit: null);
      } catch (_) {}
      expect(captured, isNotNull);
      final request = captured!;
      expect(request.method, 'DELETE');
      expect(request.path, '/todos/dust-id');
      expect(request.queryParameters, isA<Map<String, dynamic>>());
      expect(request.headers, isA<Map<String, dynamic>>());
      expect(request.headers['x-api-version'], equals('2026-05'));
    });  });}
