/// Audit stamp.
mixin AuditStamp {
  /// Returns the audit label.
  String auditLabel() => 'audited';
}

/// Catalog node model for the product showcase example.
class CatalogNode {
  /// Creates a [CatalogNode].
  const CatalogNode();
}
