name: state_management_prototype
description: Dust-first Flutter state management prototype.
publish_to: none
version: 0.1.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  derive_serde_annotation:
    path: ../../packages/derive_serde_annotation
  dust_http_client_annotation:
    path: ../../packages/dust_http_client_annotation

dependency_overrides:
  derive_annotation:
    path: ../../packages/derive_annotation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
