// GENERATED CODE - DO NOT MODIFY BY HAND
// coverage:ignore-file
// ignore_for_file: type=lint
// ignore_for_file: unused_element, unused_import, deprecated_member_use
// ignore_for_file: deprecated_member_use_from_same_package
// ignore_for_file: use_function_type_syntax_for_parameters
// ignore_for_file: unnecessary_const, avoid_init_to_null
// ignore_for_file: invalid_override_different_default_values_named
// ignore_for_file: prefer_expression_function_bodies
// ignore_for_file: annotate_overrides, invalid_annotation_target
// ignore_for_file: unnecessary_question_mark
// Generated by dust.

part of 'account_model.dart';

const DeepCollectionEquality _accountViewScopesEquality = DeepCollectionEquality();

mixin _$Account {
  @override
  String toString() {
    final self = this as Account;
    return 'Account('
        'id: ${self.id}, '
        'email: ${self.email}, '
        'passwordHash: ${self.passwordHash}, '
        'passwordSalt: ${self.passwordSalt}, '
        'scopes: ${self.scopes}'
        ')';
  }

  @override
  bool operator ==(Object other) {
    final self = this as Account;
    return identical(this, other) ||
        other is Account &&
            runtimeType == other.runtimeType &&
            other.id == self.id &&
            other.email == self.email &&
            other.passwordHash == self.passwordHash &&
            other.passwordSalt == self.passwordSalt &&
            other.scopes == self.scopes;
  }

  @override
  int get hashCode {
    final self = this as Account;
    return Object.hashAll([
      runtimeType,
      self.id,
      self.email,
      self.passwordHash,
      self.passwordSalt,
      self.scopes,
    ]);
  }
}

mixin _$AccountView implements Serializable {
  @override
  String toString() {
    final self = this as AccountView;
    return 'AccountView('
        'id: ${self.id}, '
        'email: ${self.email}, '
        'scopes: ${self.scopes}'
        ')';
  }

  @override
  bool operator ==(Object other) {
    final self = this as AccountView;
    return identical(this, other) ||
        other is AccountView &&
            runtimeType == other.runtimeType &&
            other.id == self.id &&
            other.email == self.email &&
            _accountViewScopesEquality.equals(other.scopes, self.scopes);
  }

  @override
  int get hashCode {
    final self = this as AccountView;
    return Object.hashAll([
      runtimeType,
      self.id,
      self.email,
      _accountViewScopesEquality.hash(self.scopes),
    ]);
  }

  Map<String, Object?> serialize() =>
      _$AccountViewSerialize(this as AccountView);

  Map<String, Object?> toJson() => serialize();
}

final class $AccountViewSerializer implements Serializer<AccountView, Map<String, Object?>> {
  const $AccountViewSerializer();

  @override
  Map<String, Object?> serialize(AccountView value) => _$AccountViewSerialize(value);
}
Account _$AccountFromRow(Row row) {
  return Account(
      id: row.read<int>('id'),
      email: row.read<String>('email'),
      passwordHash: row.read<String>('password_hash'),
      passwordSalt: row.read<String>('password_salt'),
      scopes: row.read<String>('scopes'),
    );
}

/// Row deserializer for [Account].
final class $AccountRowDeserializer implements RowDeserializer<Account> {
  const $AccountRowDeserializer();

  @override
  Account deserialize(Row row) => _$AccountFromRow(row);
}

/// Typed row query terminals for [Account].
///
/// Resolved from the static type of the receiver, so a row type with no
/// `FromRow` has no terminals and the call does not compile.
extension $AccountQuery on QueryAs<Account> {
  /// Fetches exactly one row.
  Future<Account> fetchOne(DatabaseExecutor db) =>
      fetchOneWith(db, _$AccountFromRow);

  /// Fetches zero or one row.
  Future<Account?> fetchOptional(DatabaseExecutor db) =>
      fetchOptionalWith(db, _$AccountFromRow);

  /// Fetches every row.
  Future<List<Account>> fetchAll(DatabaseExecutor db) =>
      fetchAllWith(db, _$AccountFromRow);
}

Map<String, Object?> _$AccountViewSerialize(AccountView instance) {
  return <String, Object?>{
    'id': instance.id,
    'email': instance.email,
    'scopes': instance.scopes
        .map((item) => item)
        .toList(),
  };
}

Map<String, Object?> _$AccountViewToJson(AccountView instance) =>
    _$AccountViewSerialize(instance);
