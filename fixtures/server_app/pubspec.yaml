name: server_app
description: Fixture for the Dust server plugin, hand-emitted until it exists.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.6.0 <4.0.0"

resolution: workspace

dependencies:
  dust_dart:
    path: ../../packages/dust_dart
  dust_server:
    path: ../../packages/dust_server

dev_dependencies:
  http: ^1.2.2
  test: ^1.26.0
  very_good_analysis: ^10.3.0
